[project]
name = "campushub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "slxshx", email = "leon.linss@outlook.de" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg>=3.3.4",
    "pydantic-settings>=2.15.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
campushub = "campushub:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-sugar>=1.1.1",
]
